import React from 'react';
import { FormattedMessage } from 'react-intl';

const Home = () => {
    return(
        <div>
            <FormattedMessage id='home' defaultMessage="Edit the files and save to reload" />
        </div>
    )
}

export default Home;